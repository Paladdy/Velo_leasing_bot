# 💳 Интеграция ЮKassa

## Обзор

Интеграция с ЮKassa для онлайн-оплаты продления аренды велосипедов.

## Тарифы

| Период | Цена | Дней |
|--------|------|------|
| 2 недели | 6 500 ₽ | 14 |
| Месяц | 12 600 ₽ | 30 |

## Настройка

### 1. Переменные окружения

Добавьте в `.env` файл одну из конфигураций:

**Вариант A: OAuth токен (рекомендуется)**
```env
YOOKASSA_OAUTH_TOKEN=eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCJ9...ваш_токен
```

**Вариант B: Basic Auth**
```env
YOOKASSA_SHOP_ID=ваш_shop_id
YOOKASSA_SECRET_KEY=ваш_secret_key
```

### 2. Проверка статуса платежа

**Текущий режим работы: Polling (без webhook)**

Клиент сам проверяет статус платежа кнопкой "✅ Я оплатил".
Это работает без публичного домена и сервера!

```
Клиент → Оплата на сайте ЮKassa → Нажимает "Я оплатил" → Бот проверяет статус → Продление
```

### 3. [TODO] Webhook сервер (опционально, на будущее)

> ⚠️ **Пока не используется.** Требует публичный домен с HTTPS.

Для мгновенных уведомлений об оплате от ЮKassa:

```env
ENABLE_WEBHOOK_SERVER=true
WEBHOOK_PORT=8080
```

**Webhook URL для личного кабинета ЮKassa:**
```
https://ваш-домен.ru/webhook/yookassa
```

## Функционал

### Для клиентов

1. **📋 Мои аренды** - просмотр активных аренд
2. **Продление онлайн** - выбор тарифа и оплата через ЮKassa
3. **Проверка статуса** - после оплаты можно проверить статус платежа

### Процесс оплаты

```
Клиент -> "📋 Мои аренды" -> Выбор аренды -> Выбор тарифа -> Оплата -> Автопродление
```

1. Клиент нажимает "📋 Мои аренды"
2. Выбирает активную аренду для продления
3. Выбирает тариф (2 недели или месяц)
4. Переходит на страницу оплаты ЮKassa
5. После успешной оплаты аренда автоматически продлевается

## Файлы

| Файл | Описание |
|------|----------|
| `services/payment_service.py` | Сервис интеграции с API ЮKassa |
| `services/webhook_server.py` | Webhook сервер для уведомлений |
| `bot/handlers/client/extension.py` | Обработчики продления аренды |

## API ЮKassa

### Создание платежа
```python
from services.payment_service import rental_extension_service

payment = await rental_extension_service.create_extension_payment(
    rental_id=123,
    tariff_key="biweekly",  # или "monthly"
    telegram_user_id=456
)

# URL для оплаты
confirmation_url = payment["confirmation"]["confirmation_url"]
```

### Проверка статуса
```python
status = await rental_extension_service.check_payment_status(payment_id)
# "pending", "succeeded", "canceled"
```

## Docker

```yaml
# docker-compose.yml
environment:
  ENABLE_WEBHOOK_SERVER: "true"
  WEBHOOK_PORT: "8080"
ports:
  - "8081:8080"  # Webhook endpoint
```

## Безопасность

- Webhook URL должен быть защищён HTTPS
- Токены хранить в переменных окружения, не в коде
- В продакшне использовать reverse proxy (nginx) перед webhook сервером

## Тестирование

1. Создайте тестовый магазин в [личном кабинете ЮKassa](https://yookassa.ru)
2. Используйте тестовые данные карты для проверки
3. Проверьте получение webhook уведомлений

## Планы на будущее

- [ ] Интеграция выкупа велосипеда
- [ ] Подписки с автоплатежами
- [ ] История платежей в профиле

